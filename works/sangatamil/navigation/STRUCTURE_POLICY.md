# Gate H Derived Navigation Policy

## Non-destructive rule

The `navigation/` tree is downstream metadata only. It must never be treated as authority over canonical page wording.

## Authority

1. canonical page records — physical/source-preservation layer;
2. `indexes/section-register.md` — Gate-E section identity/range authority;
3. `indexes/source-citation-register.md` — Gate-F printed provenance authority;
4. `GATE_G_METADATA_STATUS_AUDIT.md` — final metadata/status audit state;
5. this navigation layer — derived discoverability only.

## Composite provenance rows

Gate F counts **115 formal provenance units** across **105 formal register rows** because seven rows explicitly contain multiple formal units:

- scan 147 — 2;
- scan 163 — 4;
- scan 401 — 2;
- scan 462 — 2;
- scan 474 — 2;
- scan 475 — 3;
- scan 482 — 2.

Gate H splits only those source-declared composites into stable leaf IDs. It does not split note-only records into artificial formal units.

## No inference

Do not infer missing source metadata, normalize printed labels, rename stable section slugs merely to match later display-heading corrections, or derive quotation start/end scans beyond what the Gate-F boundary note states.
